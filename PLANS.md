# PLANS

本プロジェクトは P0/P1/P2 の段階で実装を進める。
checkpoint は原則 P0末尾のみ。

- P0: 実行基盤・パイプライン骨格・最小スモーク（依存追加なし）
- P1: 最有力手法（SDF narrow band + Vn閉包 + level-set）を最小実装し、SEM特徴とMAP校正まで
- P2: 拡張（Teacher-Student蒸留、表面GNN非局所、Neural Operator、多忠実度/BO/SBIなど）

詳細は `plans/EXECPLAN.md` を参照。
